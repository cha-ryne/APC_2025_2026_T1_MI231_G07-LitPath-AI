from django.contrib import admin

# Register your models here when you create them
# Example:
# from .models import SearchHistory, Bookmark, DocumentCache
# 
# @admin.register(SearchHistory)
# class SearchHistoryAdmin(admin.ModelAdmin):
#     list_display = ('session_id', 'query_text', 'created_at')
#     search_fields = ('query_text', 'session_id')
#     list_filter = ('created_at',)
#
# @admin.register(Bookmark)
# class BookmarkAdmin(admin.ModelAdmin):
#     list_display = ('session_id', 'document_cache', 'created_at')
#     list_filter = ('created_at',)
